{
  "name": "download-link-xray",
  "version": "0.1.2",
  "private": true,
  "description": "MV3 Chrome extension that highlights likely real download links and inspects executable risk.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
